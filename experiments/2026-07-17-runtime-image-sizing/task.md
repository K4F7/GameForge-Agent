# 运行时图片尺寸归一化任务

验证 Seedream 等生图服务输出的大尺寸图片进入 Phaser 后，不会把显示对象和 Arcade Physics 碰撞体放大到不可玩。

验收条件：

1. 玩家和危险物固定显示为 32×32，收集物固定显示为 24×24；
2. 物理体尺寸与显示尺寸一致；
3. 程序化占位纹理和 Manifest 图片走同一尺寸规则；
4. 256×256 PNG 作为三个角色素材时，生成工程可检查、可构建、可运行；
5. 系统 Chrome 截图与 telemetry 证明场景正常；
6. Bun 整仓门禁通过。
