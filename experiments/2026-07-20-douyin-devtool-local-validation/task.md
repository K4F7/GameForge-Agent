# 实验任务

在抖音开发者工具中导入受管 `douyin-mini-game` 离线产物，完成本地编译器与模拟器检查；不得执行 preview、上传、真机调试、提审或发布。

验收要求：

- DevTool 能识别 `release/bytedancegame` 工程；
- 本地普通编译结束且问题计数为零；
- 模拟器能渲染场景与 HUD；
- 发现的运行时异常需要修复并重新验证；
- 不记录账号、AppID、二维码、本机绝对路径或其他用户私有状态。
