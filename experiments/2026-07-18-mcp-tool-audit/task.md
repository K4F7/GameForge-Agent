# MCP 工具调用审计

为真实 CodeArts/OpenCode MCP 会话增加默认关闭的安全审计：只记录调用顺序、工具名、开始时间、耗时和成功/失败，不记录参数、结果或错误正文；benchmark capture 可显式导入审计并机械生成工具摘要。

验收条件：生产 stdio MCP 真实调用生成审计；并发完成仍按调用顺序；文件有界、0600、原子替换且拒绝复用；截断审计不可导入；真实 Relay record 的工具统计不再为 null；整仓门禁通过。
