# Workbench 设计视图任务

将 Workbench 中未连接的场景结构和地图占位页替换为真实运行数据驱动的只读设计视图。

验收条件：

1. 场景树只消费已验证的 `spec.ready` 与 `asset.ready` 状态；
2. 明确区分已绑定资产与程序化/静音回退；
3. 五种 genre 都有确定性、有界的模板地图示意；
4. 地图不伪装成真实关卡文件，不产生编辑事件；
5. 真实浏览器可切换两个视图且控制台无错误；
6. 使用 Bun 完成单测、严格检查、构建和整仓门禁。
