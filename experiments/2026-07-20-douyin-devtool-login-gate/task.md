# 实验任务

在不执行平台 preview、上传、提审或发布的前提下，确认当前 Windows 环境是否已安装并能启动抖音开发者工具，并尝试进入小游戏本地导入前置流程。

验收边界：

- 只读取本机安装信息并启动已安装客户端；
- 不输入手机号、邮箱、密码、验证码或其他凭据；
- 不代替用户扫码登录；
- 不向平台传输 GameForge 工程；
- 只有登录后实际完成本地导入、编译器和模拟器检查，才可关闭路线图对应待办。
