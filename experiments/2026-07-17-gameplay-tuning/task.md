# 核心玩法参数任务

让 GameSpec 不只选择 genre 和文案，还能有界控制收集物数量、危险物数量、初始生命和移动速度，并证明生成运行时真实消费这些值。

验收条件：

1. 旧 GameSpec 保持兼容；
2. 新百炼严格 JSON Schema 必须输出四个参数；
3. 契约拒绝越界数量和速度；
4. arena 与 platformer 都使用参数；
5. Workbench 场景树、地图与规格面板显示真实参数；
6. 新生成项目经真实系统 Chrome 读取 telemetry，证明数量、生命与速度生效；
7. Bun 目标测试和整仓门禁通过。
